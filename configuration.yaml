
# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:

# Text to speech
tts:
  - platform: google_translate

# logger:
#   default: warn
#   logs:
#     custom_components.mitemp_bt: debug

media_player:
  - platform: androidtv
    name: TV (Stofa)
    host: 10.0.0.5
#  - platform: androidtv
#    name: TV (Svefnó)
#    host: 10.0.0.7

light:
  - platform: group
    name: Eldhúsborð
    entities:
      - light.eldhusbord_bulb1
      - light.eldhusbord_bulb2
      - light.eldhusbord_bulb3

yeelight:
  devices:
    10.0.0.91:
      name: Baðspegill
      transition: 1000
      use_music_mode: true
      save_on_change: true

shelly:

influxdb:
  host: localhost
  database: home_assistant
  username: !secret influxdb_username
  password: !secret influxdb_password
  max_retries: 3
  default_measurement: state

group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
sensor: !include_dir_merge_list sensors/